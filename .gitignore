__pycache__/
*.pyc
.venv/
.env
data/real/
*.pdf
# real extractions / gold sets are proprietary-compilation data — never commit
data/**/real_*.json
# real Project-1 gold/extraction files kept at repo root — never commit (PSX no-redistribution)
gold_set.json
gold_set_full.json
*.egg-info/
# local handoff / context notes — keep private (portfolio strategy + P1 internal notes)
PROJECT_CONTEXT.md
